import { Meteor } from 'meteor/meteor';

import '../imports/api/fileSchemas';
import '../imports/api/rules';
import '../imports/api/notifications';

import '../imports/api/notifications/restAPI';

Meteor.startup(() => {
  // code to run on server at startup
});
